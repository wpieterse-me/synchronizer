#!/bin/bash

THIS_SCRIPT_DIRECTORY="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
ROOT_SCRIPT_DIRECTORY="${THIS_SCRIPT_DIRECTORY}/../../../.."

source "${ROOT_SCRIPT_DIRECTORY}/shared"

function Process {
    ProcessCommonArguments $@
    ProcessCommonPre

    ProcessGitRepositories \
        --remote-base-path "https://https://github.com/pocl" \
        --local-base-path "${SERVER_PC_DEVELOPMENT_PATH}/https://github.com/pocl" \
        --repository "pocl" --branch "master"

    ProcessCommonPost
}

Process $@
