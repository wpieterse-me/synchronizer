#!/bin/bash

THIS_SCRIPT_DIRECTORY="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
ROOT_SCRIPT_DIRECTORY="${THIS_SCRIPT_DIRECTORY}/../../../.."

source "${ROOT_SCRIPT_DIRECTORY}/shared"

function Process {
    ProcessCommonArguments $@
    ProcessCommonPre

    ProcessGitRepositories \
        --remote-base-path "https://github.com/OpenXRay" \
        --local-base-path "${SERVER_PC_DEVELOPMENT_PATH}/github.com/OpenXRay" \
        --repository "xray-16" --branch "dev"

    ProcessCommonPost
}

Process $@
